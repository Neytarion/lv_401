pipeline {
    agent any
    stages {
        stage('Creating database node') {
            steps {
                build job: 'build_db'
            }
        }
        stage('Creating tomcat node') {
            steps {
                build job: 'installtomcatserver'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}